from django.apps import AppConfig

class LiquidacionConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.liquidacion'
    verbose_name = 'Liquidación de Sueldos y Jornales'
